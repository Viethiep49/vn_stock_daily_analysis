[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    integration: Tests gọi API thật (cần internet, cần --run-integration)

filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
